CREATE TABLE IF NOT EXISTS category(
  id_category INT NOT NULL AUTO_INCREMENT,
  name VARCHAR(45) NOT NULL,
  PRIMARY KEY (`id_category`));
insert into category values (2,'DC');
SELECT  1 from dual;

